FROM node:24-alpine

WORKDIR /app

ARG VITE_API_BASE_URL=http://localhost:18000/api
ENV VITE_API_BASE_URL=${VITE_API_BASE_URL}

COPY package.json package-lock.json /app/
RUN npm ci

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
